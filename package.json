{
	"name": "mypages",
	"version": "1.0.0",
	"description": "Collection of my research",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "markdown-preprocessor --src ./src --dest ./docs --generate-index"
	},
	"author": "James Harrison",
	"license": "ISC",
	"dependencies": {
		
	},
	"devDependencies": {
		"markdown-preprocessor": "^1.4.0"
	}
}
