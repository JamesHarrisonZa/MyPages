{
	"name": "mypages",
	"version": "1.0.0",
	"description": "Collection of my research",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "markdown-preprocessor --src ./src --dest ./docs --generate-index --generate-header --generate-footer"
	},
	"author": "James Harrison",
	"license": "ISC",
	"dependencies": {},
	"devDependencies": {
		"markdown-preprocessor": "^2.0.0"
	}
}
